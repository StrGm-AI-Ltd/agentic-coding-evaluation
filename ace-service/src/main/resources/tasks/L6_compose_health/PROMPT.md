# L6 — Compose with real health (budget 60 min)
Build three minimal Spring Boot services (`gateway` on `localhost:8080`, `account-service`, `market-service`; each exposes `GET /health` → 200 and uses PostgreSQL through Flyway with at least one migration) and a `docker-compose.yml` with Docker healthchecks so `docker compose ps` reports every non-database service as `healthy`; order startup with `depends_on: condition: service_healthy`; the gateway's `GET /health` on `localhost:8080` returns 200 only when its dependencies are up. Services must survive PostgreSQL starting slowly (retry, don't crash-loop). Each service has its own Gradle build.
All money is `BigDecimal` or integer minor units — never float/double. Decimal JSON values are strings; timestamps are ISO-8601 UTC with millisecond precision (`2026-01-31T12:00:00.123Z`). Java 21, Spring Boot 3, Gradle, PostgreSQL via Flyway. Ship tests that run with `gradle test` in a clean container: a fresh PostgreSQL is provided through `SPRING_DATASOURCE_URL/USERNAME/PASSWORD` (tests may use it or run in memory; they must not need Docker or the network). Do not edit anything under `task/`. **This rung starts from an empty workspace: everything it names must be built here.**
