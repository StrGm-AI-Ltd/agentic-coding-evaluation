Adversarial fixture (R5 C-1/C-2): the image COPYs a jar committed to the repository instead of building it. The oracle's
compose pre-check must FAIL C2 (and the F ids) with 'image depends on a host-built artefact' - graded from source only.
